1 // What is the function of the secondary memory in a computer?
Secondary memory is used to store data for long periods of time even when power to the computer is gone. Examples of this are Hard Drives and USB sticks.

2 // What is a program?

3 // What is the difference between a compiler and an interpreter?
An interpreter can process each line of code immediately upon parsing it. A compiler requires the entire file to process the source code, convert it to machine language and save it as a file (.exe, .dll, etc.,) for future use.

4 // Which of the following contains "machine code"?

5 // What is wrong with the following code?

6 // Where in the computer is a variable such as "x" stored after the following Python line flashes?

7 // What will the following program print out:

8 // Explain each of the following using an example of a human capability: (1) Central processing unit, (2) Main Memory,
 (3) Secondary Memory, (4) Input Device, and (5) Output Device. For example, "What is the human equivalent to a
 Central Processing Unit"?

9 // How do you fix a "Syntax Error"?